# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/

# SwiftPM
.build/
.swiftpm/
# Coverage counters a test run leaves in the directory it was started from
*.profraw

# Scratch directories for local verification output
tmp/
var/

# Screenshots as the capture run produced them. What is delivered lives in fastlane/screenshots.
scripts/generate_screenshots/artifacts/

# Local-only values (signing identifiers, notarization credentials)
.envrc

# macOS
.DS_Store
